---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} >
     <title>Meet Kenichi</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Georgia', serif;
      background: #fff;
      color: #111;
      overflow: hidden; /* Prevents scrolling */
      height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      background-color: #eee;
      padding: 1rem;
      text-align: center;
      box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
      z-index: 1000;
    }

    .header-button {
      display: inline-block;
      background-color: #444;
      color: #fff;
      padding: 0.6rem 1.2rem;
      border: none;
      border-radius: 4px;
      text-decoration: none;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .header-button:hover {
      background-color: #222;
    }

    main {
      display: flex;
      flex: 1;
      width: 100vw;
      height: calc(100vh - 140px); /* Adjust for header + footer */
    }

    .section {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      font-size: 2rem;
      transition: background-color 0.3s ease;
    }

    .section a {
      text-decoration: none;
      color: inherit;
      width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .sotai {
      background-color: #f5f5f5;
    }

    .book {
      background-color: #e0e0e0;
    }

    .about {
      background-color: #d0d0d0;
    }

    .section a:hover {
      background-color: rgba(0, 0, 0, 0.05);
    }

    footer {
      background-color: #eee;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
      color: #555;
      box-shadow: 0 -1px 4px rgba(0, 0, 0, 0.05);
    }

    @media (max-width: 768px) {
      main {
        flex-direction: column;
        height: auto;
        overflow-y: auto;
      }
      .section {
        height: 33.33vh;
      }
    }
  </style>
</head>
<body>
  <header>
    <a href="/contact" class="header-button">Book a Session</a>
  </header>

  <main>
    <section class="section sotai">
      <a href="/sotai">What is Sotai?</a>
    </section>
    <section class="section book">
      <a href="/contact">Book a Session</a>
    </section>
    <section class="section about">
      <a href="/about">About Kenichi</a>
    </section>
  </main>

  <footer>
    &copy; 2025 Kenichi Bodyworks LLC. All rights reserved.
  </footer>
</body>
</html>
